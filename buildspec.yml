version: 0.2

env:
  variables:
    PROJECT: HoodooDonuts
    DOTNET_FRAMEWORK: 4.8.4
phases:
  build:
    commands:
      - nuget restore
     
artifacts:
  files:
    - '**/*'
  base-directory: 'C:\codebuild\artifacts\_PublishedWebsites\${env:PROJECT}_Package\Archive\'